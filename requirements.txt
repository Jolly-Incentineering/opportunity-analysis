# Required — core workflow will not function without these
openpyxl
python-pptx
requests

# Optional — gracefully skipped if not installed
# edgartools     # SEC filing lookups
# pypdf          # PDF metadata editing
# playwright     # Cheat sheet PDF generation (requires browser install)
